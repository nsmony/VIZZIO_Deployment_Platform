server {
    listen 80;
    server_name localhost;

    client_max_body_size 80g;

    location /api/ {
        proxy_pass http://127.0.0.1:4000;
        proxy_http_version 1.1;
        # Stream uploads once. Nginx request buffering otherwise adds a second
        # full disk write before Node can process the package.
        proxy_request_buffering off;
        proxy_buffering off;
        proxy_connect_timeout 30s;
        proxy_send_timeout 24h;
        proxy_read_timeout 24h;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Backend returns X-Accel-Redirect to this internal location after
    # validating the signed download token and user access.
    location /_vizzio_downloads/ {
        internal;
        alias /var/www/vizzio/builds/;
        sendfile on;
        tcp_nopush on;
        aio threads;
        directio 8m;
        output_buffers 1 1m;
        add_header Accept-Ranges bytes always;
        add_header Cache-Control "private, no-store" always;
    }

    error_log /var/log/nginx/vizzio-error.log;
    access_log /var/log/nginx/vizzio-access.log;
}
